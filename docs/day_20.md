# День 20. Что происходит до `main` и после него

## Опора на материалы ВШЭ

`Slides2026-0xC.pdf`: `_start`, `__libc_start_main`, начальный стек, `argc/argv/envp`, `.init_array`, `.fini_array`, `objdump`, этапы сборки и запуска программы.

## Входные знания

Перед началом проверь, что ты можешь:

- знать этапы сборки из дня 02;
- понимать `call/ret`, стековый фрейм и CDECL.

Если один из пунктов не получается объяснить или показать на маленьком примере, вернись к [Дню 02](/day_02) и [Дню 17](/day_17).

---

## За 30 секунд

- `main` не является первой инструкцией процесса.
- Loader отображает executable и библиотеки в память, после чего управление получает стартовый код-код.
- `_start` подготавливает вызов среда выполнения, а среда выполнения вызывает `main`.
- `objdump` показывает не только пользовательскую функцию, но и служебный код запуска.

## Минимум после главы

Ты должен уметь:

- объяснить путь `exec → загрузчик → _start → среда выполнения → main`;
- узнать стартовый код-фрагменты в дизассемблировании;
- отличить пользовательский код от служебного;
- объяснить, почему `global main` работает при линковке с libc.

Можно пока не заучивать:

- динамический линкер во всех деталях;
- ручную реализацию полноценного CRT.

---

## Зачем этот день

В C++ кажется, что программа начинается с `main`.

```cpp
int main() {
    return 0;
}
```

Это удобная иллюзия. На самом деле до `main` уже произошло довольно много событий:

- ОС загрузила executable;
- подготовила память процесса;
- подготовила начальный стек;
- стартовый код вызвал libc;
- libc вызвала init-функции;
- и только потом управление дошло до твоего `main`.

Сегодня мы убираем последнюю большую магию: **что значит “программа запустилась”**.

---

## Главная мысль

`main` — это не первая инструкция процесса.

`main` — это пользовательская функция, которую вызывает стартовый код среда выполнения.

Упрощённый путь:

```text
OS загрузчик
   |
   v
_start
   |
   v
__libc_start_main(...)
   |
   v
конструкторы / init
   |
   v
main(argc, argv, envp)
   |
   v
destructors / fini
   |
   v
exit
```

Когда мы пишем:

```asm
global main
```

мы говорим не “вот первая инструкция процесса”, а:

> вот функция `main`, которую C среда выполнения должен найти и вызвать.

---

## 1. Ещё раз: путь сборки

До запуска программа проходит несколько стадий.

```text
source code
   |
   | preprocessing / compilation
   v
assembly
   |
   | assembling
   v
object file (.o)
   |
   | linking
   v
executable
   |
   | loading
   v
process in memory
```

Для NASM:

```bash
nasm -f elf32 main.asm -o main.o
gcc -m32 -no-pie main.o -o main
./main
```

Стадии:

| Stage | Что получается |
|---|---|
| assembling | object file `.o` |
| linking | executable |
| loading | процесс в памяти |
| стартовый код | `_start`, libc, init |
| пользовательский код | `main` |

---

## 2. Что делает загрузчик

Loader — часть ОС, которая загружает программу в память.

Он примерно делает:

- читает исполняемый файл;
- создаёт адресное пространство процесса;
- размещает секции кода и данных;
- подключает динамические библиотеки;
- готовит стек;
- передаёт управление стартовой точке программы.

Упрощённая память процесса:

```text
higher addresses
+--------------------------+
| stack                    |
| grows downward           |
+--------------------------+
|                          |
| mmap / shared libraries  |
|                          |
+--------------------------+
| heap                     |
| grows upward             |
+--------------------------+
| .bss                     |
+--------------------------+
| .data                    |
+--------------------------+
| .rodata                  |
+--------------------------+
| .text                    |
+--------------------------+
lower addresses
```

Это не точная карта для каждой системы, но хорошая учебная модель.

---

## 3. Начальный стек

Когда программа только стартует, ОС кладёт на стек данные запуска.

Обычно там можно найти:

- `argc` — количество аргументов командной строки;
- `argv` — массив строк аргументов;
- `envp` — переменные окружения;
- служебные данные для dynamic загрузчик.

Пример запуска:

```bash
./main hello 123
```

Тогда C видит:

```c
argc = 3
argv[0] = "./main"
argv[1] = "hello"
argv[2] = "123"
```

В `main` это приходит уже в удобном виде:

```c
int main(int argc, char** argv, char** envp)
```

Но до этого стартовый код должен забрать данные из начального стека и вызвать `main` по правилам ABI.

---

## 4. `_start`

`_start` — реальная стартовая точка обычной C-программы.

Ты обычно её не пишешь. Она приходит из стартовые файлы, которые подключает компоновщик через `gcc`.

Именно поэтому мы линковали так:

```bash
gcc -m32 -no-pie main.o -o main
```

а не пытались вручную сразу сделать всё через `ld`.

`_start` подготавливает вызов libc и в итоге приводит к вызову `main`.

Если собрать совсем без libc и самому написать `_start`, можно сделать программу без `main`, но это уже отдельная низкоуровневая история.

---

## 5. `__libc_start_main`

`__libc_start_main` — функция libc, которая помогает запустить C-программу правильно.

Она отвечает за такие вещи:

- подготовить окружение C среда выполнения;
- вызвать init-функции;
- вызвать твой `main`;
- обработать результат `main`;
- вызвать финализацию и `exit`.

То есть твой `main` находится внутри более большой схемы.

```text
_start -> __libc_start_main -> main -> exit
```

Для учебной цели не нужно знать все внутренности libc. Важно понимать, почему в дизассемблировании есть код до `main`.

---

## 6. `.init_array` и `.fini_array`

В C++ до `main` могут выполняться конструкторы глобальных объектов.

После `main` могут выполняться деструкторы.

Для этого используются специальные секции, например:

```text
.init_array
.fini_array
```

Упрощённо:

```text
.init_array  -> функции, которые надо вызвать до main
.fini_array  -> функции, которые надо вызвать после main / при exit
```

Пример C++:

```cpp
struct A {
    A()  { /* before main */ }
    ~A() { /* after main */ }
};

A global;

int main() {
    return 0;
}
```

Конструктор `global` выполнится до входа в `main`.

---

## 7. Что показывает `objdump`

Команды:

```bash
objdump -h main
```

Показывает секции.

```bash
objdump -d -M intel main
```

Показывает дизассемблирование.

```bash
objdump -s -j .data main
```

Показывает содержимое `.data`.

```bash
objdump -s -j .init_array main
objdump -s -j .fini_array main
```

Показывает содержимое init/fini секций, если они есть.

---

## 8. Почему в `objdump` много “лишнего”

Когда ты дизассемблируешь программу:

```bash
objdump -d -M intel main
```

ты видишь не только свой `main`.

Там могут быть:

- `_start`;
- PLT/GOT-код для динамических вызовов;
- стартовый код/teardown код;
- служебные функции libc;
- выравнивание;
- куски, сгенерированные компилятором/линкером.

Это нормально.

Для начала ищи свою функцию:

```text
<main>:
```

В `less` удобно:

```text
/main
```

---

## 9. Почему `global main` работает

В NASM:

```asm
global main

main:
    xor eax, eax
    ret
```

`global main` экспортирует символ `main`, чтобы компоновщик и C среда выполнения могли его найти.

Если забыть `global main`, линковщик может сказать что-то вроде:

```text
undefined reference to main
```

Потому что стартовый код-код ожидает, что пользовательская функция `main` существует.

---

## 10. Что если написать `_start` самому

Можно сделать программу без libc:

```asm
section .text
global _start

_start:
    mov eax, 1      ; sys_exit on old 32-bit Linux syscall ABI
    xor ebx, ebx    ; status 0
    int 0x80
```

Но в этом курсе основной путь другой:

```text
global main + gcc -m32 -no-pie + libc
```

Почему?

Потому что задачи используют `scanf`, `printf`, CDECL и обычное C-окружение. Нам выгоднее понимать `main`, а не вручную писать весь среда выполнения.

---

## Практика
#### 1. Найди `_start`

Собери программу и выполни:

```bash
objdump -d -M intel main | less
```

Найди `<_start>`.

<details>
<summary>Подсказка</summary>

В `less` нажми `/`, введи `_start`, Enter.

</details>

#### 2. Найди `main`

Найди `<main>` в том же дизассемблировании.

<details>
<summary>Ответ</summary>

Там будет твой код: `xor eax,eax`, `ret` или инструкции из твоей программы.

</details>

#### 3. Почему `main` не первая инструкция?

<details>
<summary>Ответ</summary>

Потому что сначала управление получает стартовый код (`_start`), который подготавливает C среда выполнения и вызывает `main`.

</details>

#### 4. Что такое `.init_array`?

<details>
<summary>Ответ</summary>

Секция с адресами функций, которые среда выполнения вызывает до `main`, например для глобальных конструкторов C++.

</details>

#### 5. Почему мы используем `gcc -m32 -no-pie` для линковки?

<details>
<summary>Ответ</summary>

Потому что `gcc` как компоновщик driver подключает нужный стартовый код-код и libc, а `-m32` говорит линковать 32-битную программу.

</details>

---

## 12. Типовые ошибки

| Ошибка | Почему плохо |
|---|---|
| думать, что `main` — первая инструкция процесса | сначала работает загрузчик/стартовый код/среда выполнения |
| искать только свой код в `objdump` и пугаться лишнего | дизассемблируется весь executable |
| забыть `global main` | стартовый код-код не найдёт `main` |
| путать `.o` и исполняемый файл | объектный файл ещё нельзя запустить как программу |
| линковать через `ld` без понимания стартовый код/libc | легко получить ошибки или неправильный запуск |
| думать, что конструкторы вызываются из `main` | глобальные конструкторы вызываются до `main` средой выполнения |

---

## Чеклист
После этого дня ты должен уметь:

- [ ] объяснить путь «исходный код → объектный файл → исполняемый файл → процесс»;
- [ ] объяснить, что среда выполнения вызывает `main`;
- [ ] знать роль `_start` и `__libc_start_main` на уровне идеи;
- [ ] объяснить, что такое начальный стек, `argc`, `argv`, `envp`;
- [ ] объяснить `.init_array` и `.fini_array`;
- [ ] использовать `objdump` для просмотра секций и кода;
- [ ] не пугаться служебного кода в дизассемблировании.

Если можешь объяснить, почему твой `main` не первая инструкция, день усвоен.

---

## Следующий шаг

1. Реши [TR-20 в рабочей тетради](/transfer_workbook#tr-20) без просмотра ответа.
2. После законченной попытки открой [диагностический ключ TR-20](/transfer_keys#key-tr-20).
3. Запиши нарушенный инвариант и минимальный контрпример в журнал ошибок.
4. Если модель верна — переходи дальше; если нет — вернись к указанному материалу и затем реши новый вариант.
