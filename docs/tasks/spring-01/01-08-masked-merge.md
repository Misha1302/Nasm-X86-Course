# 01-8. Объединение по маске

## Условие

Даны три 32-битные беззнаковые числа `a`, `b`, `c` в шестнадцатеричном виде.

Нужно вывести `d`, где каждый бит выбирается так:

```text
если c[i] = 1 -> d[i] = a[i]
если c[i] = 0 -> d[i] = b[i]
```

Запрещены условные передачи и условное управление.

## Ввод

```text
a b c
```

Числа записаны в шестнадцатеричном виде.

## Вывод

Одно 32-битное число `d` в шестнадцатеричном виде.

## Ограничения

- все числа — 32-битные беззнаковые;
- работать надо сразу со всеми 32 битами.

## Решение

Маска `c` показывает, где брать биты из `a`.

```text
a & c
```

Маска `~c` показывает, где брать биты из `b`.

```text
b & ~c
```

Обе части не пересекаются, значит их можно склеить через `or`:

```text
d = (a & c) | (b & ~c)
```

## Схема на NASM

```asm
mov eax, [a]
and eax, [c]

mov ecx, [c]
not ecx
and ecx, [b]

or eax, ecx
```

Для ввода/вывода удобно:

```asm
fmtIn  db "%x %x %x", 0
fmtOut db "%x", 10, 0
```

## Ручные проверки

| `c` | Ответ |
|---|---|
| `0` | полностью `b` |
| `FFFFFFFF` | полностью `a` |
| `FF00FF00` | часть битов из `a`, часть из `b` |

## Ошибки

| Ошибка | Почему плохо |
|---|---|
| `(a & ~c) | (b & c)` | выбор будет наоборот |
| забыть `not c` для `b` | обе части будут брать одни и те же позиции |
| печатать через `%d` | задача требует шестнадцатеричный вывод |

## Где в курсе

- [День 10 — задачи без ветвлений](/day_10)
- [Маски для кода без ветвлений](/patterns/branchless)
