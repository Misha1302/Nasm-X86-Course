# День 24. Объекты C++: поля, `this` и виртуальные вызовы

## Опора на материалы ВШЭ

`Slides2026-0xD.pdf`: объект как структура, служебные поля, `vptr`, VMT, RTTI, виртуальные методы, скрытый `this`, `thiscall` и низкоуровневое представление объектов C++.

## Входные знания

Перед началом проверь, что ты можешь:

- считать смещения полей структуры и учитывать выравнивание;
- отличать прямой вызов от косвенного;
- читать кадр стека CDECL.

Если один из пунктов не получается объяснить на маленьком примере, вернись к [Дню 19](/day_19), [Дню 14](/day_14) или [Дню 17](/day_17).

---

## За 30 секунд

- Поле объекта лежит по постоянному смещению от адреса объекта.
- Метод получает адрес объекта через скрытый параметр `this`.
- Объект с виртуальными методами обычно содержит указатель `vptr` на таблицу виртуальных функций.
- Виртуальный вызов читает адрес функции из таблицы и выполняет косвенный `call`.
- Точное расположение объекта зависит от ABI и компилятора.

## Минимум после главы

Ты должен уметь:

- распознать скрытый параметр `this`;
- вычислить смещение поля;
- объяснить связь `vptr` и таблицы виртуальных функций;
- отличить прямой вызов от косвенного и виртуального;
- отделить подтверждённые факты листинга от гипотезы о C++.

Можно пока не заучивать:

- полные правила Itanium ABI и MSVC ABI;
- сложное множественное и виртуальное наследование;
- внутреннее устройство RTTI.

---

## Зачем этот день

В исходном коде C++ мы видим классы, поля и методы:

```cpp
obj.field
obj.method()
obj.virtualMethod()
```

Процессор видит только память, адреса и вызовы функций. Поэтому на низком уровне:

```text
объект              = область памяти с полями
обычный метод       = функция со скрытым параметром this
виртуальный метод   = функция, адрес которой выбирается из таблицы
```

Эта модель помогает читать листинги. Но она не доказывает точный исходный код: похожую схему может создавать и обычная таблица указателей на функции.

---

## Главная мысль

Обычный объект похож на структуру. Например:

```cpp
struct Point {
    int x;
    int y;
};
```

При типичном расположении IA-32:

```text
адрес объекта + 0  -> x
адрес объекта + 4  -> y
```

Имена `x` и `y` в памяти не хранятся. Компилятор заранее заменяет их смещениями.

---

## 1. Обычный метод и скрытый `this`

Метод:

```cpp
int Point::sum() {
    return x + y;
}
```

можно мысленно представить как обычную функцию:

```cpp
int Point_sum(Point* this) {
    return this->x + this->y;
}
```

В листинге это может выглядеть так:

```asm
; ecx условно содержит this
mov eax, [ecx]
add eax, [ecx + 4]
ret
```

Подтверждённые факты:

- один адрес используется как основа;
- читаются два 32-битных значения по смещениям `0` и `4`;
- сумма возвращается через `eax`.

Гипотеза: адрес в `ecx` — это `this`, а два значения — поля объекта. Для уверенного вывода нужен контекст ABI и вызова.

---

## 2. Где передаётся `this`

Место передачи `this` зависит от ABI и компилятора. В 32-битном коде можно встретить `thiscall`, где `this` передаётся через `ecx`. В другом соглашении тот же адрес может лежать в стеке.

Поэтому формулируй вывод осторожно:

```text
Факт: ecx используется как указатель на область памяти.
Гипотеза: ecx содержит this.
```

Не называй гипотезу доказанным фактом только из-за знакомого шаблона.

---

## 3. Виртуальные методы и `vptr`

Рассмотрим класс:

```cpp
struct Base {
    virtual int f();
    int x;
};
```

Объекту нужен способ выбрать реализацию `f`. Обычно в объект добавляется служебный указатель `vptr`:

```text
Объект Base:

+------------------+
| vptr             | ----+
+------------------+     |
| x                |     |
+------------------+     |
                         v
                  +------------------+
                  | адрес Base::f    |
                  +------------------+
                  | адрес другого    |
                  | виртуального     |
                  | метода           |
                  +------------------+
```

Таблица виртуальных функций хранится отдельно. Сам объект хранит только указатель на неё и обычные поля.

---

## 4. Виртуальный вызов

Исходный код:

```cpp
Base* p = ...;
p->f();
```

Упрощённая схема:

```asm
mov eax, [p]          ; адрес объекта
mov edx, [eax]        ; возможный vptr
call dword [edx]      ; вызов функции из первой ячейки таблицы
```

Если метод находится не в первой ячейке:

```asm
call dword [edx + 4]
```

Главное отличие:

| Вызов | Откуда берётся адрес функции |
|---|---|
| прямой | записан в самой инструкции `call` |
| косвенный | прочитан из регистра или памяти |
| виртуальный | обычно прочитан из таблицы виртуальных функций объекта |

Любой виртуальный вызов является косвенным, но не любой косвенный вызов является виртуальным.

---

## 5. Наследование

Простой случай:

```cpp
struct Base {
    int a;
};

struct Derived : Base {
    int b;
};
```

Объект `Derived` может начинаться с базовой части:

```text
+------------------+
| Base::a          |
+------------------+
| Derived::b       |
+------------------+
```

При множественном или виртуальном наследовании расположение сложнее. В этом курсе достаточно помнить: наследование влияет на смещения и может добавлять служебные указатели.

---

::: info Обзорная тема
Для ядра главы обязательны скрытый `this`, смещения полей, `vptr`, таблица виртуальных функций и виртуальный вызов. RTTI рассматривается только на уровне узнавания.
:::

## 6. RTTI

RTTI — служебные сведения о типах во время выполнения. Они нужны, например, для `dynamic_cast` и `typeid`.

Для курса достаточно понимать:

```text
RTTI — это данные, созданные компилятором, а не магия языка.
```

Точное устройство RTTI зависит от ABI.

---

## 7. Как читать подозрительный листинг

Фрагмент:

```asm
mov eax, [ecx]
call dword [eax + 8]
```

Подтверждает только следующее:

1. `ecx` используется как указатель;
2. по адресу `[ecx]` читается ещё один указатель;
3. адрес вызываемой функции читается по смещению `8`;
4. вызов является косвенным.

Возможная интерпретация:

```text
ecx      = this
[ecx]    = vptr
[eax+8]  = ячейка таблицы виртуальных функций
```

Но ту же машинную схему может дать таблица обратных вызовов. Поэтому в ответе разделяй факты и гипотезы.

---

## 8. Что где хранится

| Понятие C++ | Низкоуровневое представление |
|---|---|
| поле | байты по постоянному смещению |
| объект | область памяти с полями |
| обычный метод | функция со скрытым параметром `this` |
| виртуальный метод | функция, адрес которой выбирается из таблицы |
| `this` | указатель на текущий объект |
| наследование | базовая часть и дополнительные поля объекта |
| RTTI | служебные сведения о типах |
| имя метода | обычно не хранится в каждом объекте |

---

## Практика

#### 1. Нарисуй возможное расположение объекта

```cpp
struct A {
    virtual void f();
    int x;
    int y;
};
```

<details>
<summary>Ответ</summary>

Один из типичных вариантов:

```text
+------------------+
| vptr             |
+------------------+
| x                |
+------------------+
| y                |
+------------------+
```

Точное расположение зависит от ABI.

</details>

#### 2. Что такое скрытый `this`?

<details>
<summary>Ответ</summary>

Это адрес объекта, для которого вызван метод.

</details>

#### 3. Чем виртуальный вызов отличается от прямого?

<details>
<summary>Ответ</summary>

При прямом вызове адрес функции записан в инструкции. При виртуальном вызове адрес обычно читается из таблицы виртуальных функций объекта.

</details>

#### 4. Хранятся ли методы внутри каждого объекта?

<details>
<summary>Ответ</summary>

Нет. Код методов хранится отдельно. Объект содержит поля и, возможно, служебные указатели.

</details>

#### 5. Что может означать `mov eax, [ecx+4]` внутри метода?

<details>
<summary>Ответ</summary>

Чтение 32-битного поля по смещению `4`, если `ecx` содержит адрес объекта.

</details>

#### 6. Отдели факт от гипотезы

```asm
mov eax, [ecx]
call dword [eax+8]
```

Назови два подтверждённых факта и одну возможную интерпретацию на C++.

<details>
<summary>Ответ</summary>

Факты: `ecx` разыменовывается; адрес вызова читается из памяти по смещению `8`. Гипотеза: `ecx` содержит `this`, `[ecx]` — `vptr`, а `[eax+8]` — ячейка таблицы виртуальных функций.

</details>

---

## Типовые ошибки

| Ошибка | Почему неверно |
|---|---|
| считать, что объект хранит копии методов | код методов хранится отдельно |
| считать `vptr` обычным полем исходного класса | это служебная деталь реализации |
| забывать скрытый `this` | непонятно, к какому объекту относятся поля |
| называть любой косвенный вызов виртуальным | это может быть обычная таблица указателей на функции |
| переносить расположение между ABI без проверки | смещения и служебные поля могут отличаться |
| выдавать гипотезу за факт | листинг часто допускает несколько исходных представлений |

---

## Чеклист

После этого дня ты должен уметь:

- [ ] объяснить объект как область памяти с полями;
- [ ] читать поля по смещениям;
- [ ] объяснить скрытый `this`;
- [ ] нарисовать связь `vptr` и таблицы виртуальных функций;
- [ ] отличить прямой вызов от косвенного и виртуального;
- [ ] объяснить назначение RTTI на уровне идеи;
- [ ] отделить факт машинного кода от гипотезы о C++;
- [ ] помнить, что точное расположение зависит от ABI.

Если ты можешь объяснить `p->virtualMethod()` через адрес объекта, `vptr` и таблицу функций, день усвоен.

---

## Следующий шаг

1. Реши [TR-24 в рабочей тетради](/transfer_workbook#tr-24) без просмотра ответа.
2. После законченной попытки открой [диагностический ключ TR-24](/transfer_keys#key-tr-24).
3. Запиши нарушенный инвариант и минимальный контрпример в журнал ошибок.
4. Если модель верна — переходи дальше. Если нет, вернись к указанному материалу и затем реши новый вариант.
